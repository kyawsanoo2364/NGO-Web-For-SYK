import { motion } from "framer-motion";

const BallAnimation = ({}) => {
  return (
    <>
      <motion.div className="absolute top-14  size-20 rounded-full bg-gradient-to-r from-green-600 to-white"></motion.div>
    </>
  );
};
export default BallAnimation;
